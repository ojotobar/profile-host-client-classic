import { MonthYearOrNaPipe } from './month-year-or-na.pipe';

describe('MonthYearOrNaPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthYearOrNaPipe();
    expect(pipe).toBeTruthy();
  });
});
